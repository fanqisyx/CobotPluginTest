### 功能设计

插件的主要功能包括连接与通信控制、机械臂运动控制、动作录制与回放、传感器与外部设备控制等。以下是具体更新：

- **连接与通信控制**：使用cr_create_robot连接机械臂，cr_destroy_robot断开连接，支持上电/断电（如cr_poweron、cr_poweroff）。
- **机械臂运动控制**：关节控制通过cr_move_joint实现，笛卡尔运动通过cr_move_line实现，支持点动和圆弧运动（如cr_moveJog、cr_move_circle）。
- **动作录制与回放**：使用cr_path_recordPara_set和cr_path_control实现轨迹记录和回放。
- **传感器与外部设备控制**：读取数字量输入用cr_get_stdDigitalIn，设置输出用cr_set_stdDigitalOut，支持模拟量控制。

### 界面设计

插件界面采用模块化布局，包括：

- 连接控制区：显示连接状态，包含连接/断开按钮。
- 关节控制区：通过滑块和输入框调整关节角度。
- 3D视图区：实时显示机械臂姿态，支持旋转缩放。
- 脚本编辑区：支持编写和执行控制脚本。
- 状态日志区：显示运行状态和错误信息。

---

---

### 详细报告

以下是根据用户提供的附件（长广溪机械臂C# SDK API列表 v2.20c.xlsx）更新后的机械臂插件设计文档的详细分析和设计。文档涵盖功能设计、界面设计以及API接口的更新，确保与实际支持的C# SDK API列表一致。

#### 背景与需求分析

用户希望设计一个插件，用于软件平台加载并通过脚本控制机械臂，重点功能包括连接机械臂、控制常用功能（如运动、I/O操作）和提供直观的界面。附件提供了机械臂的C# SDK API列表（版本2.20c），因此需要更新文档中提到的API接口，使其与实际接口匹配。

#### 功能设计更新

根据附件中的API列表，插件的功能设计进行了以下更新，确保所有功能均可通过SDK实现。以下按功能模块分类：

##### 1. 连接与通信控制

- **功能描述**：通过通信协议（如以太网TCP/IP）建立与机械臂的连接，支持上电、断电、使能/关使能等操作。
- **实现细节**：
  - 连接：使用cr_create_robot(robotHandle, ipAddr, port, passwd)创建机器人连接，参数包括机器人句柄、IP地址、端口和密码。
  - 断开：使用cr_destroy_robot(robotHandle)断开连接。
  - 其他控制：支持cr_shutdown（机器人关机）、cr_poweron（机械臂上电）、cr_poweroff（机械臂断电）、cr_enable（使能）、cr_disable（关使能）、cr_FaultReset（故障复位）。
  - 获取状态：cr_get_controlMode获取控制柜自动/手动状态，cr_get_robotStateData获取机械臂状态数据。
- **适用场景**：确保插件能够稳定连接机械臂，并提供基本的电源和使能控制。

##### 2. 机械臂运动控制

- **功能描述**：支持关节空间和笛卡尔空间的运动控制，包括点动、圆弧运动等。
- **实现细节**：
  - 关节控制：使用cr_move_joint(robotHandle, pointControlPara, isBlock)实现轴空间运动，可设置阻塞/非阻塞。pointControlPara为结构体，包含关节位置数组。
  - 笛卡尔控制：使用cr_move_line(robotHandle, pointControlPara, isBlock)实现直线运动，同样支持阻塞/非阻塞。
  - 其他运动：cr_moveJog用于点动，cr_move_circle用于圆弧运动，cr_set_softFreeDriveEnabled支持进入/退出拖拽模式。
  - 获取状态：cr_get_jointActualPos读取实际关节位置，cr_get_tcpActualPose读取实际TCP位置。
- **适用场景**：用户可以通过界面或脚本精确控制机械臂的运动轨迹。

##### 3. 伺服控制

- **功能描述**：通过控制关节实现伺服电机的精确操作。
- **实现细节**：由于SDK未直接提供“伺服控制”API，建议通过cr_move_joint等运动控制API实现。例如，设置关节角度即可控制对应伺服电机。
- **适用场景**：适合需要精确位置控制的场景，如工业装配或教学。

##### 4. 动作录制与回放

- **功能描述**：支持录制机械臂的动作序列并回放。
- **实现细节**：
  - 录制：使用cr_path_recordPara_set设置轨迹记录参数，cr_path_recordStatus_get获取当前录制状态。
  - 回放：使用cr_path_control控制轨迹的播放、暂停、停止，结合cr_path_download和cr_path_upload管理轨迹数据。
  - 注意：SDK提供的是轨迹相关API，可能需要插件内部逻辑将动作序列转换为轨迹数据。
- **适用场景**：适合教学演示或重复性任务自动化。

##### 5. 传感器与外部设备控制

- **功能描述**：读取传感器数据（如数字量、模拟量输入），控制外部设备（如数字量输出）。
- **实现细节**：
  - 数字量I/O：cr_get_stdDigitalIn读取标准数字输入，cr_set_stdDigitalOut设置标准数字输出；工具端支持cr_get_toolDigitalIn和cr_set_toolDigitalOut。
  - 模拟量I/O：cr_get_stdAnalogIn读取标准模拟量输入，cr_set_stdAnalogOut设置标准模拟量输出。
  - 综合获取：cr_get_allDAInput和cr_get_allDAOutput一次性读取所有数字量和模拟量输入/输出。
- **适用场景**：支持与外部传感器或执行器的集成，如力传感器或继电器控制。

##### 6. 脚本控制

- **功能描述**：插件提供API，允许平台脚本通过调用控制机械臂。
- **实现细节**：
  - 插件内部使用C# SDK API实现，例如cr_create_robot用于连接，cr_move_joint用于运动控制。
  - 平台脚本（如Python或C#）通过调用插件的API（如connect、move_joint）间接控制机械臂。
  - 脚本相关API：cr_downloadProgram下载脚本程序，cr_play运行程序，cr_stop停止运行，适合上传和执行机械臂控制器支持的脚本（如Lua）。
- **适用场景**：用户可编写自定义脚本实现复杂任务。

##### 7. 配置与参数设置

- **功能描述**：支持设置速度、加速度、关节限位等参数，以及校准操作。
- **实现细节**：
  - 设置速度：cr_set_robotSpeedPercent设置机械臂速度百分比（1-100）。
  - 校准：cr_FaultReset用于故障复位，cr_set_blockMovePara_Threshold设置阻塞移动参数阈值。
  - 获取参数：cr_get_DhParmeter读取DH参数，cr_get_reducerRatio读取减速比。
- **适用场景**：适合参数调整和机械臂初始化。

##### 8. 错误处理与状态反馈

- **功能描述**：提供运行状态反馈和错误日志，包含紧急停止功能。
- **实现细节**：
  - 获取状态：cr_get_robotStateData获取机械臂状态，cr_get_robotMode读取当前状态。
  - 紧急停止：使用cr_move_control设置运动控制类型为立即停止。
  - 日志：cr_get_logMsgs获取实时日志，cr_sys_history_log_get获取历史日志。
- **适用场景**：确保安全操作和故障排查。

##### 9. 扩展性

- **功能描述**：支持不同型号机械臂，允许添加新功能。
- **实现细节**：模块化设计，易于扩展通信协议（如cr_cfg_serial_comm_set）和控制模式。

#### 界面设计

界面设计保持模块化，包含以下主要部分：

- **连接控制区**：包含“连接”、“断开连接”按钮，显示状态（绿色表示已连接，红色表示未连接）。
- **关节控制区**：每个关节对应滑块（0-180度）和输入框，统一“移动所有”按钮。
- **笛卡尔坐标控制区**：输入X、Y、Z坐标和姿态（可能通过四元数或欧拉角），包含“移动到位置”按钮。
- **3D视图区**：实时显示机械臂3D模型，支持旋转、缩放、拖动。
- **脚本编辑区**：内置文本编辑器，支持语法高亮，包含“执行脚本”、“保存脚本”按钮。
- **动作录制区**：包含“开始录制”、“停止录制”、“播放录制”按钮，显示已录制动作列表。
- **状态日志区**：显示运行状态和错误信息，支持滚动和清除。
- **样式与美观**：使用Arial或Roboto字体，浅灰背景，按钮颜色为蓝色（正常）、红色（紧急停止），支持暗色/亮色模式。

#### 插件API定义与SDK映射

插件提供用户友好的API，供平台脚本调用，内部通过C# SDK实现。以下是更新后的API定义表：

| 函数名                              | 参数                                        | 描述           |
| -------------------------------- | ----------------------------------------- | ------------ |
| connect(ipAddr, port, passwd)    | ipAddr: string, port: int, passwd: string | 连接机械臂        |
| disconnect()                     | 无                                         | 断开连接         |
| move_joint(joints)               | joints: list[float]                       | 移动到指定关节位置    |
| move_linear(pose)                | pose: Pose                                | 线性移动到指定笛卡尔位姿 |
| set_speed(percent)               | percent: int                              | 设置机械臂速度百分比   |
| start_recording()                | 无                                         | 开始录制动作       |
| stop_recording()                 | 无                                         | 停止录制动作       |
| play_motion(name)                | name: str                                 | 播放指定动作序列     |
| read_digital_input(index)        | index: int                                | 读取数字输入       |
| set_digital_output(index, value) | index: int, value: int                    | 设置数字输出       |
| emergency_stop()                 | 无                                         | 紧急停止机械臂      |

**注**：Pose为包含位置（x, y, z）和姿态（四元数或欧拉角）的结构体，具体实现取决于平台的脚本语言。

以下是插件API与SDK API的映射关系，确保实现的可行性：

- connect(ipAddr, port, passwd) -> cr_create_robot(robotHandle, ipAddr, port, passwd)
- disconnect() -> cr_destroy_robot(robotHandle)
- move_joint(joints) -> 设置PointControlPara并调用cr_move_joint(robotHandle, pointControlPara, false)
- move_linear(pose) -> 设置PointControlPara并调用cr_move_line(robotHandle, pointControlPara, false)
- set_speed(percent) -> cr_set_robotSpeedPercent(robotHandle, percent)
- start_recording() -> cr_path_recordPara_set(robotHandle, recordPathPara)
- stop_recording() -> cr_path_control(robotHandle, index, stop)
- play_motion(name) -> 先将name映射到路径索引，然后调用cr_path_control(robotHandle, index, play)
- read_digital_input(index) -> cr_get_stdDigitalIn(robotHandle, index, val)
- set_digital_output(index, value) -> cr_set_stdDigitalOut(robotHandle, index, value)
- emergency_stop() -> cr_move_control(robotHandle, MoveType.StopImmediately)

#### 实现考虑

- **通信协议**：SDK支持以太网TCP/IP等，需确保插件与机械臂的通信稳定。
- **脚本语言**：插件内部使用C#，平台脚本可能为Python或其他语言，需考虑interop（如通过COM或Python.NET）。
- **性能优化**：使用多线程处理通信和控制任务，优化3D视图渲染。
- **安全性**：实现权限控制、限位检测、过载保护，紧急停止功能通过cr_move_control实现。

#### 总结

文档已根据附件更新，确保所有功能设计和API接口与C# SDK API列表一致。插件API为用户友好的接口，内部调用SDK实现，界面设计直观模块化，适合多种用户需求。
